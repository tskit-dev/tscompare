coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

flag_management:
  default_rules:
    carryforward: true

flags:
  python-tests:
    paths:
      - tscompare/

ignore:
  - tests/
