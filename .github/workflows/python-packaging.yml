name: Packaging

on:
  pull_request:
  merge_group:

jobs:
  packaging:
    permissions:
      contents: read
    uses: tskit-dev/.github/.github/workflows/python-packaging.yml@v18
    with:
      pyproject-directory: .
